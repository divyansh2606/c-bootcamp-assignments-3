#include<stdio.h>
int main()
{
    int n,x;
    n=12,x=13;
    if(n>x)
      printf("%d is greater ",n);
      else if(x>n)
       printf("%d is greater",x);
       else
        printf("%d",n);

    return 0;
}
