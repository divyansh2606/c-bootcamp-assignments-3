#include<stdio.h>
int main()
{
    int n;
printf("enter no");
scanf("%d",&n);
if(n%3==0&&n%2==0)
    printf("no is divisible by  3 and 2 both");
else
    printf("no is  not divisible by 3 and 2 both");
return 0;
}
